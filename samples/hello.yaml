openapi: 3.0.3
info:
  title: Speckify
  description: Speckify
  version: 1.0.0
servers:
  - url: 'https://speckify.com'
paths:
  /ping:
    get:
      responses:
        200:
          description: Ping endpoint
          content:
            text/plain:
              schema:
                type: string
                example: pong
